% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bounds_BLP.R
\name{bounds_BLP}
\alias{bounds_BLP}
\title{This function compute the DGM bounds on the BLP for all the different coefficients.}
\usage{
bounds_BLP(
  Xnc,
  Y,
  Xc_x = NULL,
  Xc_y = NULL,
  Wc_x = NULL,
  Wc_y = NULL,
  w_x = NULL,
  w_y = NULL,
  dimXc = 0,
  dimW = 0,
  values = NULL,
  full = FALSE,
  FW = FALSE,
  discrete = FALSE,
  unchanged = FALSE,
  factor = FALSE,
  K_sel = 0,
  dataset = 0,
  O_only = FALSE
)
}
\arguments{
\item{Xnc}{Matrix of non-conditioning covariates (predictor variables).}

\item{Y}{Vector or matrix of outcome/dependent variable values.}

\item{Xc_x}{Matrix of conditioning covariates for the right-hand side data.}

\item{Xc_y}{Matrix of conditioning covariates for the left-hand side data.}

\item{Wc_x}{Optional matrix of additional covariates (weights/factors) for right-hand side data.}

\item{Wc_y}{Optional matrix of additional covariates (weights/factors) for left-hand side data.}

\item{w_x}{Optional vector of weights for right-hand side data (default NULL).}

\item{w_y}{Optional vector of weights for left-hand side data (default NULL).}

\item{dimXc}{Integer: number of conditioning covariates (default 0).}

\item{dimW}{Integer: number of weight/factor covariates (default 0).}

\item{values}{Optional list or matrix of values used for discrete/factor covariates (default NULL).}

\item{full}{Logical: whether to compute full BLP bounds (default FALSE).}

\item{FW}{Logical: whether to apply Frisch-Waugh-type adjustment (default FALSE).}

\item{discrete}{Logical: whether to treat conditioning variables as discrete (default FALSE).}

\item{unchanged}{Logical: whether to retain all values when discretizing (default FALSE).}

\item{factor}{Logical: whether to treat W covariates as factors (default FALSE).}

\item{K_sel}{Integer: number of clusters for discretization (default 0, auto-selected).}

\item{dataset}{Integer flag indicating if we use propensity score weighting (default 0).}

\item{O_only}{Logical, whether to compute bounds only for outside regressors (default FALSE).}
}
\value{
A matrix of dimension `(number of covariates) x 2`, where each row represents
the lower and upper bounds for a corresponding regression coefficient.
}
\description{
This function compute the DGM bounds on the BLP for all the different coefficients.
}
\details{
The function performs the following operations:
1. Normalizes weights (`w_x`, `w_y`) and computes combined weights.
2. Determines whether a direct computation is possible (same sample size, no weights).
3. If no conditioning or factor variables exist, performs direct regression-based bounds.
4. If conditioning/factor covariates are present:
   a. Applies Frisch-Waugh adjustment if `FW = TRUE`.
   b. Discretizes or clusters `W` if `discrete = TRUE`.
   c. Handles special cases for `exact = 1` or `exact = 2`.
   d. Computes weighted residuals, projections, and Earth Mover's Distance (EMD)  bounds.
5. Returns the computed lower and upper bounds for each covariate.
}
\examples{
\dontrun{
# Simple usage with random data
Xnc <- matrix(rnorm(100), 50, 2)
Y <- rnorm(50)
results <- bounds_BLP(Xnc, Y, dimXc = 0, dimW = 0)
print(results)
}

}
