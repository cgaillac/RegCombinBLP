% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_BLP.R
\name{compute_BLP}
\alias{compute_BLP}
\title{Compute Bounds on the BLP}
\usage{
compute_BLP(
  sample1 = NULL,
  Xc_x,
  Xnc,
  Xc_y,
  Wc_x,
  Wc_y,
  Y,
  w_x = NULL,
  w_y = NULL,
  dimXc,
  dimW,
  dimXnc,
  values = NULL,
  full = FALSE,
  FW = FALSE,
  discrete = FALSE,
  unchanged = TRUE,
  rule = 1,
  factor = FALSE,
  K_sel = 0,
  dataset = 0,
  O_only = FALSE
)
}
\arguments{
\item{sample1}{Optional vector of indices for subsampling or bootstrapping (default NULL).}

\item{Xc_x}{Matrix of conditioning covariates for the right-hand side data.}

\item{Xnc}{Matrix of  outside regressors (non-commonly observed) (used in regression).}

\item{Xc_y}{Matrix of conditioning covariates for the left-hand side data.}

\item{Wc_x}{Optional matrix of additional covariates (weights/factors) for right-hand side data.}

\item{Wc_y}{Optional matrix of additional covariates (weights/factors) for left-hand side data.}

\item{Y}{Vector or matrix of outcome/dependent variable values.}

\item{w_x}{Optional vector of weights for right-hand side data (default NULL).}

\item{w_y}{Optional vector of weights for left-hand side data (default NULL).}

\item{dimXc}{Integer: number of conditioning covariates.}

\item{dimW}{Integer: number of weight/factor covariates.}

\item{dimXnc}{Integer: number of non-conditioning covariates.}

\item{values}{Optional values used in bounds computation (default NULL).}

\item{full}{Logical: whether to use full computation in BLP bounds (default FALSE).}

\item{FW}{Logical: whether to apply Frisch-Waugh adjustments (default FALSE).}

\item{discrete}{Logical: whether to treat conditioning variables as discrete (default FALSE).}

\item{unchanged}{Logical: whether to keep all values when discretizing (default TRUE).}

\item{rule}{Numeric: sampling rule for bootstrap/subsampling (default 1).}

\item{factor}{Logical: whether to treat W covariates as factors (default FALSE).}

\item{K_sel}{Integer: number of clusters for discrete/factor variables (default 0, auto-selected).}

\item{dataset}{Integer: dataset type flag (default 0).}

\item{O_only}{Logical, whether to compute bounds only for outside regressors (default FALSE).}
}
\value{
A list or object containing BLP bounds. Returns DGM BLP bounds,#
 including hull points and influence function data (if applicable).
}
\description{
Computes bounds on the BLP coefficients, optionally
using different types of conditioning variables, weights, bootstrapping, and factor/discrete adjustments.
}
\details{
The function proceeds in the following steps:
1. **Resampling**: If `sample1` is provided, performs subsampling or bootstrapping
   for predictor (`Xnc`, `Xc_x`, `Wc_x`) and outcome (`Y`, `Xc_y`, `Wc_y`) matrices.
2. **Weight normalization**: Weights (`w_x`, `w_y`) are normalized to sum to 1 for the selected sample.
3. **Bounds computation**: Depending on `DP`:
   a. `DP = TRUE`: Calls `bounds_Pacini_Xc()` to compute Pacini-style bounds.
   b. `DP = FALSE`: Calls `bounds_BLP()` to compute standard BLP bounds with optional full, discrete, and factor adjustments.
4. Returns the computed bounds and related outputs.
}
\examples{
\dontrun{
# Basic usage with random data
results <- compute_BLP(
  sample1 = NULL,
  Xc_x = matrix(rnorm(100), 50, 2),
  Xnc = matrix(rnorm(50), 50, 1),
  Xc_y = matrix(rnorm(100), 50, 2),
  Wc_x = NULL,
  Wc_y = NULL,
  Y = rnorm(50),
  dimXc = 2,
  dimW = 0,
  dimXnc = 1
)
}

}
